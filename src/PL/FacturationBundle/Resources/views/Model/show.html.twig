{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Famille</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libfam</th>
                <td>{{ entity.libFam }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('famille') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('famille_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
